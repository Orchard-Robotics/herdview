# Feature requests

Tabled ideas — documented so they aren't lost, not yet scheduled.

- [friendly-path-url.md](friendly-path-url.md) — reach herdview at
  `https://myhost.tailnet.ts.net/herdview` (Tailscale Serve `--set-path`
  + making herdview base-path-aware), enabling `myhost/<tool>` for multiple
  tools under one host.
