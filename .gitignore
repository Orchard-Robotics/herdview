# runtime copy placed by scripts/fetch.sh at install time.
# the committed cross-platform binaries live in bin/ and ARE tracked.
/herdview

# test harness (dev-only; never part of the plugin distribution)
test/node_modules/
test/.runtime/
test/test-results/
test/playwright-report/
